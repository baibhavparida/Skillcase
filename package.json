{
  "name": "skillcase-web",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 0.0.0.0",
    "build": "astro build",
    "preview": "astro preview --host 0.0.0.0",
    "check": "astro check",
    "cf:dev": "npm run build && wrangler pages dev dist --port 3000 --compatibility-date=2026-05-12",
    "cf:publish": "wrangler pages deploy dist --project-name skillcase-web",
    "cf:deploy": "npm run build && wrangler pages deploy dist --project-name skillcase-web",
    "cf:deploy:preview": "npm run build && wrangler pages deploy dist --project-name skillcase-web --branch preview"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@astrojs/react": "^5.0.4",
    "astro": "^6.3.1",
    "cobe": "^2.0.1",
    "lucide-react": "^1.14.0",
    "react": "^19.2.6",
    "react-dom": "^19.2.6",
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.14",
    "tailwindcss": "^3.4.17",
    "wrangler": "^4.90.1"
  },
  "overrides": {
    "vite": "^7.3.3"
  }
}
