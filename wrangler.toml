name = "skillcase-web"
compatibility_date = "2026-05-12"
pages_build_output_dir = "dist"

# This is a Cloudflare Pages project. Use:
#   wrangler pages deploy dist --project-name skillcase-web
# Do not use `wrangler deploy`; that command is for Workers and will fail here.
